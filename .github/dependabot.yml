version: 2
updates:
  # Scientific Python dependency ranges are reproducibility-sensitive and are
  # reviewed manually with numerical outputs; security updates remain enabled
  # in the repository settings.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
